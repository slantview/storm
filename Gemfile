source :rubygems

gemspec

gem "dep_selector", :group => :development, :platform => "ruby"

platforms :mswin, :mingw do
  # TODO: test with mswin, mingw
end

platforms :mingw_18 do
  # TODO: test with mingw_18
end
